# Readline init file documentation is available at:
# https://www.gnu.org/software/bash/manual/html_node/Readline-Init-File-Syntax.html

$include /etc/inputrc

# Use Vi key bindings by default.
set editing-mode vi

# Display possible completions using different colors to indicate their file
# types. The colors are determined by the environmental variable `LS_COLORS`.
set colored-stats On

# This lines sets completions to be appended by characters that indicate their
# file types reported by the `stat` system call.
set visible-stats On

# This line sets every completion which is a symbolic link to a directory to
# have a slash appended.
set mark-symlinked-directories On
